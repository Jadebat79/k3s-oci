# Copy to terraform.tfvars and fill in. Do NOT commit terraform.tfvars.

tenancy_ocid     = "ocid1.tenancy.oc1..aaaa..."
user_ocid        = "ocid1.user.oc1..aaaa..."
fingerprint      = "12:34:56:78:90:ab:cd:ef:..."
private_key      = <<EOT
-----BEGIN PRIVATE KEY-----
...paste-your-oci-api-private-key...
-----END PRIVATE KEY-----
EOT
region           = "eu-frankfurt-1"
compartment_ocid = "ocid1.compartment.oc1..aaaa..." # k3-staging (where VMs are created)

# --- Existing VCN with IGW but no subnet yet ---
use_existing_vcn             = true
existing_vcn_ocid            = "ocid1.vcn.oc1..aaaa..."
vcn_compartment_ocid         = "ocid1.compartment.oc1..bbbb..." # compartment that owns the VCN
existing_internet_gateway_id = "ocid1.internetgateway.oc1..cccc..."
subnet_cidr                  = "10.0.1.0/24" # must fit inside the VCN CIDR

ssh_public_key      = "ssh-ed25519 AAAA... yourname@host"
ssh_allowed_cidr    = "0.0.0.0/0"

# A1 "Out of host capacity"? Try a different availability domain (multi-AD regions
# like uk-london-1 have 3). Set the 1-based index and re-apply until one succeeds.
# availability_domain_number = 2
# Or pin by full name (see the `availability_domains` output):
# availability_domain = "kIdk:UK-LONDON-1-AD-2"

# 3 nodes total = 1 server + 2 agents, each 1 OCPU / 6 GB (= 3 OCPU / 18 GB).
# If capped at 2 OCPU / 12 GB, set agent_count = 1 (2 nodes total).
agent_count         = 2
instance_ocpus      = 1
instance_memory_gbs = 6

expose_http = true
